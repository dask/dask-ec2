{%- set supervisord =  "/usr/bin/supervisord" -%}
{%- set supervisorctl = "/usr/bin/supervisorctl" -%}
{%- set supervisord_dir = "/etc/supervisor/" -%}
{%- set supervisord_conf = "/etc/supervisor/supervisord.conf" -%}
{%- set conf_d = "/etc/supervisor/conf.d/" -%}
